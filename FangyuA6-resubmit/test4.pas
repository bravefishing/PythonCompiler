program four;
var x,y,z:integer;
    a,b,c,d:integer;
  procedure summit(var a:integer; x,y:integer);
  var sum:integer; {variable sum}
  begin
    sum := (x*z) div 3;
  end;
begin
  x:=a+b/c+d;
  y:= (a - b / 2)*x;
end.
